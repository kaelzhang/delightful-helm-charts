apiVersion: v1
kind: Service
metadata:
  name: {{ template "api-frontend.fullname" . }}
  labels:
    app: {{ template "api-frontend.name" . }}
    chart: {{ template "api-frontend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "api-frontend.name" . }}
    release: {{ .Release.Name }}
